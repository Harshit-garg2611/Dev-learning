export const sum = (a, b) => a + b;
export const sub = (a, b) => a - b;
export const mult = (a, b) => a * b;
export const pi = 3.14;

const obj = {
    sum: sum,
    sub: sub,
    mult: mult,
    pi: pi
}

// module.exports = obj;