<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora !</h1>
    <a href="http://localhost:8080/posts/new">Add post</a>
    <% for(post of posts) { %>
        <div class="post">
            <h3>@<%= post.username %></h3>
            <h4>Age : <%= post.age %></h4>
            <h4>Gender : <%= post.gender %></h4>
            <a href="http://localhost:8080/posts/<%=post.id%>">Click!</a>
        </div>
        <% } %>
</body>
</html>